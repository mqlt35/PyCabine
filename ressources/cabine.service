[Unit]
Description=cabine system daemon
After=network.target

[Service]
Type=simple
User=debian
Group=debian

ExecStart=/usr/bin/cabine start
ExecStop=/usr/bin/cabine stop
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target